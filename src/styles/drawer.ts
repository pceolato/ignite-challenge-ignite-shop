import { styled } from "@stitches/react";

export const DrawerContent = styled('div', {
    height: '100vh',
    position: 'fixed',
    zIndex: '1000',
    width: '400px',
    top: 0,
    right: 0,
    bottom: 0,
    background: '$gray800'
})